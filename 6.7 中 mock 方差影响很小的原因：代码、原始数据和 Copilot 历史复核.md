# 6.7 中 mock 方差影响很小的原因、正确比较方法与代码修改方案

首次复核：2026-09-15；综合更新：2026-09-16。本次重新核对两个 notebook、baofisher_k.py 和 experiments.py；前三者与前次数值审计的 SHA256 一致。本次更新的是本报告，代码修改方案尚未应用到这四个科学源文件。

阅读导航：主要原因见本节；新增单位/论文/噪声证据见 §10；正确比较方法见 §11；逐文件修改方案见 §12；核心代码示例与实施顺序见 §13–14。§1–9 保留前次复现与 Copilot 历史证据，其中原始网格数值均指当时的实现。

## 结论

**当前 σ 几乎不变，是因为代码向原解析分母加入了一项极小的散布；这并不能证明前景污染后的大尺度约束已经几乎无损地恢复。**

记 σ(F) 为 sqrt[(F^-1)_(fNL,fNL)]。当前代码使用 D_ana=(P_HI+P_N)^2、D_mock=f_bin P_HI^2，因此每个积分点的相对修正为 R=f_bin[P_HI/(P_HI+P_N)]^2。主要原因分为以下层次：

| 层次 | 已核实的原因 | 为什么影响结果 |
|---|---|---|
| 直接数值原因 | f_bin 本身很小，热噪声再乘上信号占比的平方 | 原分母几乎不变，Fisher 和 σ 自然几乎不变 |
| 统计归一化 | mock 保存的是分箱功率相对方差，6.7 用于单模分母 | f_bin 已含模式平均；在可按 1/N 缩放的近似下，需要 N_sim/2 的转换 |
| 样本处理 | 先平均两个独立噪声档位的实现，再求方差 | 本批数据的散布约被压低一半 |
| 物理噪声匹配 | 所有仪器共用单碟 RMS 生成的逐像素白噪声 mock | 体素体积、各向异性 P_N、波束、观测时间没有一致匹配 |
| 坐标与有效域 | 模拟 k 单位为 h/Mpc，真实 z 被替换；mock 越界填 0 | 用错尺度，并使一部分敏感模式隐含具有零额外误差 |
| 恢复误差的定义 | 固定同一个宇宙场，仅改变热噪声；解析侧仍保留受前景污染的大尺度响应 | 条件噪声散布尚未代表完整恢复协方差，也未证明 f_NL 响应得到恢复 |

其中，**分箱与单模归一化不一致，是可直接检验的主要实现原因；完整恢复误差与前景比较基准未建立，是科学解释上的核心限制。** aux_i/d≈3.6e-7 来自另一个辅助公式，不是主 Fisher 分母，因此不是 σ 几乎不变的直接原因。CMB 先验和矩阵求逆失效也已被排除为本批结果的主因。

前次仅改归一化的对照给出约 0.24%–5.09% 的 σ(f_NL) 变化；再处理端点统计和单位/实际红移后，对照变化为约 0.70%–13.01%。这些仍是敏感度实验，不能作为真实仪器已经校准的恢复精度。**不应通过任意放大 mock 项来强行使两组 σ 分离；应以相同数据向量、正确的参数响应和不重复计数的协方差建立比较。**

## 1. 查到的 Copilot 历史

相关历史位于 VS Code 的 Copilot transcript，而不只是现有备忘录。

- [2026-06-25 的讨论，第 94 行](/home/liuhk/.vscode-server/data/User/workspaceStorage/54453a4e73f38b4d18d43960f7d607dd-1/GitHub.copilot-chat/transcripts/8e2855ee-7156-49e2-a5d4-b84604e8f33c.jsonl:94)：确定了导出 `Var(T)/mean(T)^2`、再乘 `P_HI^2` 的做法，但没有完成分箱到单模的归一化。
- [2026-09-08 的分析，第 41 行](/home/liuhk/.vscode-server/data/User/workspaceStorage/54453a4e73f38b4d18d43960f7d607dd-1/GitHub.copilot-chat/transcripts/36701c61-4bc2-440b-b242-fd4a7798dbf0.jsonl:41) 已经指出分箱/单模归一化的疑点。
- [同一天的诊断，第 82 行](/home/liuhk/.vscode-server/data/User/workspaceStorage/54453a4e73f38b4d18d43960f7d607dd-1/GitHub.copilot-chat/transcripts/36701c61-4bc2-440b-b242-fd4a7798dbf0.jsonl:82) 复现了极小的 ratio，但没有读取原始 `Neff_bin` 并做归一化对照。后续讨论转向了辅助体素公式。

本次补上了原始 pickle、生成脚本、`Neff_bin` 和 Fisher 数值对照的核查。历史回答只作为待核实线索，其中以下解释需要纠正：

- “两个噪声档位先平均，只是降低方差估计的噪声”：它改变了被统计的随机变量，实际压低了方差本身。
- “mock 是完整宇宙实现的散布”或“本身一定包含宇宙方差”：本批生成程序对每个红移反复读取同一信号 cube，只改变高斯噪声。
- “干涉辅助函数已将 n(u) 归一化掉”或“极小 aux 比值来自整个 FOV 平均”：代码没有提供这种归一化或平均操作，不能据此赋予物理意义。
- “同一体素下干涉必定比单碟低 95 倍”：把全部基线都视作同一尺度的独立重复测量并不适用于这里的弥散 HI 信号；必须用 n(u) 和明确的成像窗口。

## 2. 为什么 σ 几乎不变

[主 notebook 的 `fisher_sum_mock_zinterp`](/home/liuhk/research/6.7-interferometer-telescopes-2.ipynb:1826) 使用

$$
D_{\rm ana}=(P_{\rm HI}+P_N)^2,\qquad
D_{\rm mock}=fP_{\rm HI}^2,\qquad
f=\frac{\mathrm{Var}(T)}{\bar T^2}.
$$

因此

$$
r\equiv\frac{D_{\rm mock}}{D_{\rm ana}}
=f\left(\frac{P_{\rm HI}}{P_{\rm HI}+P_N}\right)^2.
$$

每个积分节点对 Fisher 的贡献变为原来的 `1/(1+r)`。由此可见：

- `f` 很小，即使没有热噪声也只有小修正；
- 热噪声再通过信号占比的平方压低修正；
- CMB 先验不是两组纯 HI 结果几乎相同的原因，因为这些列本来就没加 CMB。

注意表头 `F_ana+mock` 表示“用解析方差加 mock 方差重新计算 Fisher”，不是把两个 Fisher 矩阵相加。

### 七个望远镜复现

以下是七参数边缘化后的 f_NL 误差；相对变化用本次未舍入数值计算。

| 望远镜/模式 | σ_ana | σ_ana+mock | 相对变化 |
|---|---:|---:|---:|
| SKA-MID1 干涉 | 12.2063965 | 12.2067142 | 0.002603% |
| SKA-MID1 单碟 | 4.8986357 | 4.8988383 | 0.004135% |
| CHIME | 8.9409780 | 8.9412307 | 0.002827% |
| HIRAX | 5.7124553 | 5.7125827 | 0.002232% |
| Tianlai | 20.3547810 | 20.3559017 | 0.005506% |
| cv_z0to3 配置 | 3.1389593 | 3.1390356 | 0.002430% |
| MeerKAT-B1 干涉 | 77.2472895 | 77.2524289 | 0.006653% |

对所有参数，重要的是相应 Fisher 灵敏度加权后的 r，而不是任意网格点的中位数。以 SKA 干涉的 f_NL 为例，对全部红移、并考虑边缘化后的权重计算，`<r> = 5.20813e-5`，一阶预期

$$
\frac{\Delta\sigma(f_{\rm NL})}{\sigma(f_{\rm NL})}
\simeq\frac{\langle r\rangle}{2}=2.60406\times10^{-5},
$$

与完整矩阵求逆得到的 `2.60281e-5` 一致。矩阵正定性和独立求和也已验证，结果不是矩阵求逆失效造成的。

## 3. ratio = 2.183e-6 是否合理

本次在原配置 `ttot=100000 h`、SKA 干涉、`z=1.4999823` 上复现：

| 统计量 | 数值 |
|---|---:|
| median(r) | 2.18293455e-6 |
| p90(r) | 3.33858893e-5 |
| p99(r) | 8.88384672e-5 |
| max(r) | 1.01218307e-4 |
| 插值后 f 的中位数 | 8.27455437e-5 |
| P_N/P_HI 的中位数 | 4.37042869 |
| [P_HI/(P_HI+P_N)]² 的中位数 | 0.03467226 |

后二者说明了数量级来源，但多个中位数的乘积不等于乘积的中位数。

一个具体节点，`k=0.0493121 Mpc^-1, mu=0.494949`：

$$
P_{\rm HI}=1251.56,\quad P_N=2900.54
\quad[\mathrm{mK^2\,Mpc^3}],\qquad f=5.67112\times10^{-5}.
$$

因此 `D_mock=88.83`，`D_ana=1.7240e7`，比值为 `5.1527e-6`；二者单位均为 `mK^4 Mpc^6`。

结论：**它对当前代码完全自洽，但不能作为 mock 物理效应确实微弱的证据，因为被比较的两项尚未统一统计层级。**

3990/5000 是有效积分节点数，不是物理 Fourier 模式数。940 个未被噪声掩码的节点因 mock 越界而 f=0。另 70 个非有限噪声节点实际位于积分边界：68 个在第一行 k≈0.005，2 个在最后一行 k=0.535，来自浮点边界比较。当前开启了 KMIN_EFF/KMAX_EFF，不能沿用历史回答把这 70 个节点直接称为前景楔。

## 4. 关键证据：mock 是分箱功率方差

追踪到生成代码 [make_prod_dict_hkl](/data/kcchan_rec/ding_post_mess/jupyter_mess/aureum/wx_iter_funcs.py:5809)：

```python
tf_all_bin = pposi_arr / orig_pk_bin[None, ...]
tf_avg_bin = np.mean(pposi_arr, axis=0) / orig_pk_bin
presc_arr = pposi_arr / tf_avg_bin[None, ...]
```

对固定的 `P_orig`，严格有

$$
f_j=\frac{\mathrm{Var}(T_j)}{\bar T_j^2}
=\frac{\mathrm{Var}(\widehat P_{{\rm rec},j})}{P_{{\rm orig},j}^2}.
$$

本次对原始数组直接验证了这个恒等式，偏差约在 1e-14 以下。这里的 T 是**功率比**，不是场振幅传递函数；因此乘 P_HI² 的量纲转换本身正确，问题在分箱/单模层级。

[pk_func.py](/data/kcchan_rec/ding_post_mess/jupyter_mess/aureum/pk_func.py:816) 先累计每箱功率再除以箱内 Fourier 点数，`Neff_bin` 就是该计数。对六层数据，计数相同：

| Neff_bin 的统计量 | 数值 |
|---|---:|
| min | 26 |
| median | 2896 |
| p90 | 6544 |
| p99 | 11008 |
| max | 11600 |

原生成程序明确使用 `cosmic_var_2d = 2*P_orig**2/Neff_bin`。而 [教程导出](/home/liuhk/research/fisher_1.3.2.1_DirctTF_GenNoise_AssisHKL_Tutorial.ipynb:485) 只保留 `kper_bin, kpar_bin, fisher_arr`，丢掉了 Neff。

### 怎样统一口径

高斯分箱方差为

$$
C^{\rm ana}_{jj}=\frac{2(P_j+P_{N,j})^2}{N^{\rm survey}_j}.
$$

在相同 k 分箱、散布近似随 1/N 缩放、并暂忽略箱间相关的条件下，模拟分箱的额外方差应先变换到巡天的模式数：

$$
C^{\rm extra}_{jj}=f^{\rm sim}_jP_j^2
\frac{N^{\rm sim}_j}{N^{\rm survey}_j}.
$$

将 `N_survey/2` 提到 Fisher 分子，就得到与当前积分形式相容的分母：

$$
(P_j+P_{N,j})^2+\frac{N^{\rm sim}_j}{2}f^{\rm sim}_jP_j^2.
$$

现在代码使用的是 `f_sim*P²`，少了这一步变换。`Neff/2` 中的 2 来自实场的共轭模式计数约定；`Neff` 不是模拟实现数 200。

模式数与分箱误差的标准关系也可参见 [Bull et al. (2015), §II.4、§III](https://arxiv.org/html/1405.1452#S3)。这不意味着任何类型的系统误差都能机械乘 Neff：共享的传递函数标定误差和箱间相关必须用相应协方差处理。

### 定量对照：只改变方差系数

下表固定了信号、噪声、参数偏导、积分节点和 mock 越界规则。数值都是 σ(f_NL) 相对解析结果的增加百分比。

| 望远镜 | 当前 | 先分别求端点 f 再平均 | 仅乘 Neff/2 | 两项同时做 |
|---|---:|---:|---:|---:|
| SKA 干涉 | 0.002603% | 0.005310% | 0.2415% | 0.4785% |
| SKA 单碟 | 0.004135% | 0.008319% | 0.7131% | 1.3359% |
| CHIME | 0.002827% | 0.005695% | 1.7222% | 3.2887% |
| HIRAX | 0.002232% | 0.004484% | 0.6289% | 1.2242% |
| Tianlai | 0.005506% | 0.011071% | 5.0888% | 9.6434% |
| cv_z0to3 | 0.002430% | 0.004885% | 0.7600% | 1.4270% |
| MeerKAT 干涉 | 0.006653% | 0.013679% | 0.4663% | 0.9289% |

**后三列是归一化敏感度诊断，不是已经校准好的最终预报。** 端点相对方差平均也只是一个对照，不保证等于中心噪声强度处的真实方差。

## 5. 是否按不同噪声倍数插值

**没有。** 6.7 使用 `RegularGridInterpolator((z, kper, kpar), cube)`，根本没有噪声坐标轴，也没有将望远镜噪声传给该插值器。

更准确地说，本批数据的 `std` 是直接加入信号 cube 的高斯噪声 RMS，单位 mK，不是相对于信号的噪声倍数。生成程序来自 [direct_noise_to_prod_dict.py](/data/kcchan_rec/ding_post_mess/jupyter_mess/aureum/fisher_mess/sbatch_scripts/codex_gen/direct_noise_to_prod_dict.py:41)。

| 实际模拟红移 | 噪声中心 RMS [mK] | 两个原始档位 [mK] | 导出 f 中位数 |
|---|---:|---:|---:|
| 0.5110 | 0.155875 | 0.145875 / 0.165875 | 2.5430e-4 |
| 0.9890 | 0.129886 | 0.119886 / 0.139886 | 1.8053e-4 |
| 1.5010 | 0.119222 | 0.109222 / 0.129222 | 1.2054e-4 |
| 2.0050 | 0.116758 | 0.106758 / 0.126758 | 7.9566e-5 |
| 2.5010 | 0.119402 | 0.109402 / 0.129402 | 7.3892e-5 |
| 3.0170 | 0.125579 | 0.115579 / 0.135579 | 5.9420e-5 |

中心 RMS 来自旧的 `wide` 单碟体素方差表，再在每个红移取中心±0.01 mK。6.7 却将六层红移写成 0.5、1.0、…、3.0，且所有望远镜共用它们。

### 逐实现平均为何压低方差

[教程第 4 个单元](/home/liuhk/research/fisher_1.3.2.1_DirctTF_GenNoise_AssisHKL_Tutorial.ipynb:141) 对 ndarray 执行 `(lo+hi)/2`。因此

$$
\mathrm{Var}\!\left(\frac{T_L+T_H}{2}\right)
=\frac{V_L+V_H+2\mathrm{Cov}(T_L,T_H)}{4}.
$$

两个端点的噪声逐实现相关系数中位数接近零，故 V_L≈V_H 时得到约 V/2，而不是中心噪声强度的 V。实测六层的 `f_average / [(f_low+f_high)/2]` 中位数在 0.5017–0.5037。

导出所选 `[7,15,23,31,39,47]` 均为 `N200_bin200_wdg`，实际数组 shape 是 `(200,200,200)`；代码里 `(100,200,200)` 的注释过时。N100/N200 指实现数，bin100/bin200 指不同分箱，wdg 指不同处理设置，不能把这八种配置当作独立重复样本直接平均。

### f 的数量级是否异常

作为“固定信号、噪声实现变化下的分箱相对散布”，f≈1e-4 很合理：它对应约 1% 的 RMS 分数散布，已经包含箱内许多模式的平均。

本次用原始 float64 数组重建六层后，与实际导出 float32 文件的最大相对差不超过 4.7e-6；没有发现数据导出时差了多个数量级。ddof=0 改为 ddof=1 对 200 个实现只产生 200/199 的修正，也解释不了现象。

但 f 小不说明观测噪声很小。真实生成程序对每个种子读取同一原始信号 cube，然后加入不同高斯噪声，做 k 截断、楔形处理和正值化。因此这里测得的是**固定宇宙实现下的条件散布**，不能直接等同于完整巡天协方差，也不是 200 个实现的均值估计误差。

例如 z=1.501 时，原始二维网格 `f*Neff/2` 的中位数为 0.2395；在 k<0.025 h/Mpc 的原始分箱上中位数为 0.00470。这也说明它并不简单等于完整高斯宇宙方差的 `2/Neff`。

## 6. 为什么 aux_i/d≈3.6e-7

[baofisher_k.py 的单碟辅助函数](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/baofisher_k.py:1598) 实现

$$
\sigma_{d,\mathrm{aux}}^2
=\frac{T_{\rm sys}^2 S_{\rm area}}{\Delta\nu\,t\,\theta^2N_dN_b}.
$$

[干涉辅助函数](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/baofisher_k.py:1618) 实际实现

$$
\sigma_{i,\mathrm{aux}}^2
=\frac{T_{\rm sys}^2 S_{\rm area}}{\Delta\nu\,t}\,\mathrm{FOV}^2,
\quad \mathrm{FOV}=\theta^2,\quad\theta=\lambda/D.
$$

所以不需要任何物理猜测就能得到

$$
\frac{\sigma_{i,\mathrm{aux}}^2}{\sigma_{d,\mathrm{aux}}^2}
=N_dN_b\theta^6.
$$

SKA 的 z=1.5、D=15 m、Nd=190、Nb=1 给出 θ=0.0352012，故比值为 `3.61492e-7`，完全复现。

**这个值不是可信的同体素灵敏度比。** 干涉函数的 docstring 写了 n(u)，实现没有 n(u)，也没有接收 u/k 或成像权重；更换基线分布不会改变返回值。不能仅凭函数名将其解释为完整干涉图像体素的方差，或把整个视场平均作为解释。

### Fisher 实际使用的噪声

主程序调用 [Cnoise](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/baofisher_k.py:1739)，经 `P_N=r²*rnu*Cnoise` 转成 mK² Mpc³。标准碟阵干涉分支为

$$
C_N=\frac{T_{\rm sys}^2S_{\rm area}}{n_{\rm pol}t\nu_{21}}
\frac{\lambda^4}{N_b(A_{\rm eff}\theta)^2}
\frac{e^{E_\parallel}}{n(u)}.
$$

同一标准碟阵仅切换 dish/interferom 时，共同因子消去，得到

$$
\frac{P_{N,i}}{P_{N,d}}=
\frac{N_d\theta^2}{n(u)}\,e^{-E_\perp},
\qquad E_\perp=(k_\perp/\sigma_{k\perp})^2.
$$

这里明确依赖基线密度和尺度。在 notebook 的 z≈1.5、k≈0.0493、mu≈0.495 上，比例约 0.96，和 3.6e-7 完全不同。高红移、高 k_perp 时单碟噪声由于波束反卷积指数增长，干涉可能占优；大尺度的比较则由短基线覆盖决定。

### 体素方差应怎样定义

对于指定的地图平滑窗口 W 和可观测模式集合，定义

$$
\sigma_{N,W}^2=\int\frac{d^3k}{(2\pi)^3}P_N(\mathbf k)|W(\mathbf k)|^2.
$$

不能在没有窗口、分辨率和模式选择的情况下给出唯一的干涉“体素方差”。缺测模式须通过明确的观测算子处理，不能简单把无穷噪声当成零。

对近似白噪声的单碟地图，`P_N≈sigma_vox²*V_vox`，其中 `V_vox≈(r*theta)²*rnu*dnu/nu21`。单碟辅助函数还省略了完整 Cnoise 中极化与有效面积系数；在当前 npol=2、effic=0.7 的标准碟阵下，该系数为 1.6542。因此辅助函数和 Cnoise 也不能在忽略这些约定时逐项等同。

[6.7 的方差表](/home/liuhk/research/6.7-interferometer-telescopes-2.ipynb:2233) 对所有 mode 都使用单碟 `noise_rms_per_voxel²`，信号也统一用单碟 Gaussian 窗。该表不反馈到 Fisher 求和，不能用表里的干涉“体素方差”解释 σ 的变化。

主单元是 100000 h，最后的辅助对照单元是 10000 h；Copilot 历史显示后者是单独改动的。二者的绝对噪声相差时间因子，不能混读，但这不改变 aux_i/d 的比值。

## 7. 其他与结论有关的范围限制

- 原实现把 mock 网格中心 k_perp=0.00398479、k_par=0.00342957 的数值直接用于插值，所以其数值可插值矩形要求 k≥0.00525742，而主积分从 k=0.005 开始。这些 mock 坐标后来已确认是 h/Mpc，换成 Mpc^-1 应乘 0.6766。靠近坐标轴仍会有覆盖问题；网格中心之间的插值域也不等于原始 bin 边界覆盖域。
- 在 SKA 干涉/单碟的解析 f_NL 边缘化灵敏度权重中，分别约 34.8%/49.1% 位于当前 f=0 的区域。这个百分比是参数灵敏度权重，不是节点数或独立模式数比例。
- 生成程序使用 512³ 均匀网格、boxsize=3000 h^-1 Mpc，逐像素加相同 RMS 白噪声。白噪声满足 P_N=σ_pix² V_pix。像素体积为 201.166 (Mpc/h)^3≈649.47 Mpc³，与单碟角向波束和频率通道对应的体素不同。
- joblib 本身没有 k 单位、体积、真实红移、噪声轴、窗口和 Neff。后续通过模拟配置、halo RSD 文件头和生成程序确认了 h/Mpc→Mpc^-1 的转换需求，见 §10.1；不能继续把这项列为未核实疑点。
- 解析部分的基线外延、关闭物理低 k 截断，与 mock 的 kcut/楔形/正值化处理也不是同一观测算子。

## 8. 正式修正的建议顺序

1. 明确预测的是原始 HI 功率还是处理后恢复的功率，并明确 mock 项是完整方差、额外随机误差，还是标定不确定度。
2. 保留每个噪声档位，分别计算 mean、variance 和协方差；按目标噪声插值这些统计量，不能先平均独立实现再当作目标噪声样本。
3. 统一 k 单位、体积、分箱、地图像素、信号响应和 P_N(k_perp,k_par,z)。对干涉阵，单一 RMS 通常不能替代各向异性的噪声功率谱。
4. 导出 Neff、真实 z、噪声值、bin 边界和必要元数据，在同一分箱上构造协方差后求 Fisher；或在有依据的近似下转换成当前积分所需的单模归一化。
5. 如果 mock 已经包含某种热噪声散布，要与解析噪声核对是否重复；固定信号的 mock 本身不提供完整宇宙方差。应避免随意把“完整 mock”再加到“完整解析”上。
6. 对低 k 的 mock 未覆盖区域给出有依据的模拟扩展或范围限制，不能默认额外误差已知为零。若采用箱间协方差，应先降维并验证估计稳定性：200 个实现不足以直接求逆 40000 个 bin 的经验协方差。

## 9. 验证记录与附件

- 读取全部 12 个相关原始 `N200_bin200_wdg` pickle，重建六个平均层。
- 七个望远镜、七个参数的两组纯 HI 约束均与已有 CSV 一致到原 CSV 的六位有效数字精度；最大相对差 4.3e-6，与舍入相容。
- 独立 einsum 求和与 notebook 的 mock Fisher 的最大相对矩阵差小于 1.8e-15。
- 缩放后的 Fisher 矩阵全部正定。
- 为节省重复计算，同一扰动宇宙学一次计算所有红移，再使用原 notebook 的积分和求逆；有限差分步长、信号模型和噪声保持原定义。
- [数值结果 JSON](./forecast_audit.json)、[原始 mock 审计 JSON](./raw_mock_audit.json)、[对照 CSV](./fnl_comparison.csv)、[归一化对照图](./normalization_sensitivity.png)。
- [复现脚本](./reproduce_forecasts.py) 从原 notebook 加载定义、读取 12 个原始噪声端点并重新计算；结果写到脚本所在目录。原输入的 SHA256 见 `input_sha256.json`，用于区分今后的输入改动。

## 10. 2026-09-16 综合补充：单位、论文与定量边界

### 10.1 已确认的单位和噪声匹配问题

[实际输入的 halo RSD 文件头](/mnt/loong/Raid6/2/luantch/sim_workspace/1024/L3000_N1024_fNL0/rockstar/halo_016/halos_rsd_0.0.ascii:4) 明确写出盒长 3000 Mpc/h、位置单位 Mpc/h、h=0.6766；对应 HI 和 mock 程序使用 BoxSize=3000、Ngrid=512。与 6.7 比较时：

$$
k_{\rm Mpc^{-1}}=h_{\rm sim}k_{\rm h/Mpc},\quad
P_{\rm mK^2Mpc^3}=\frac{P_{\rm mK^2(Mpc/h)^3}}{h_{\rm sim}^3}.
$$

协方差若原来以模拟功率单位保存，应除以 h_sim^6；模式计数和相对方差 f 不改变。单纯把文件中的单位字符串改掉不会完成转换。

z≈1.5 的模拟白噪声功率为 σ_pix² V_pix≈9.23 mK² Mpc³。按主 notebook 的 SKA 参数、100000 h，在 k_perp≈0.04–0.2 Mpc^-1 上，干涉热噪声功率约为 2700–3800 mK² Mpc³，即约高 300–400 倍。这是指定尺度与配置的对照，不能外推为所有 k 上固定的倍率。

因此，需要匹配 P_N(k_perp,k_parallel,z) 和观测窗口，不能把单碟一个体素的 RMS 原样放在模拟细网格上，就认为生成了对应干涉巡天的噪声。正值化是非线性的，不能用上述功率比直接乘最后导出的 f 来替代重新模拟。

同体素仪器噪声比较也必须指定窗口。对当前 SKA z≈1.5，若两张图都只保留 0.04<k_perp<0.06 Mpc^-1 的共同模式、带内等权并采用相同径向窗口，实空间带通图的方差比为 σ_int²/σ_dish²≈0.0785。该例只说明可以在明确窗口下比较；不是任意分辨率下的通用灵敏度比。原辅助函数的 3.6e-7 不具备这种含义。

### 10.2 已完成的修正敏感度实验

在前次 Fisher 缓存上，分别计算噪声端点的相对方差，使用 N_sim/2 转换，转换 k 单位，并采用实际模拟红移，得到以下对照：

| 配置 | σ_ana(f_NL) | 对照 σ(f_NL) | 相对增加 |
|---|---:|---:|---:|
| SKA 干涉 | 12.20640 | 12.29209 | 0.702% |
| SKA 单碟 | 4.89864 | 4.98721 | 1.808% |
| CHIME | 8.94098 | 9.35600 | 4.642% |
| HIRAX | 5.71246 | 5.80831 | 1.678% |
| Tianlai | 20.35478 | 23.00349 | 13.013% |
| cv_z0to3 配置 | 3.13896 | 3.19394 | 1.752% |
| MeerKAT | 77.24729 | 78.35243 | 1.431% |

SKA 干涉 z≈1.5 的正额外方差节点上，R 的 median、p90、p99、max 分别为 0.003144、0.004360、0.004979、0.005683。原实现的中位数为 2.183e-6；坐标转换也改变了正值节点的集合。

**这张表保留了旧的 mock 越界填零规则，只用于隔离实现问题。** 按 §11–13 的正式方案统一有效域后，两组基准都应重新计算，不能继续直接沿用表中的 σ_ana。两端点方差平均也只是中心噪声处的近似，需更多噪声档位验证。

### 10.3 两篇论文支持什么结论

用户提供的恢复方法来自 [Recovering Cosmic Structure with a Simple Physical Constraint，2503.20434v1](https://arxiv.org/html/2503.20434v1)。该文说明正值化利用非线性模式耦合恢复缺失信息，尤其是相位和 BAO 特征；恢复振幅、仪器噪声和统计误差仍需处理。

[Bull et al.，1405.1452v2](https://arxiv.org/html/1405.1452v2) 提供解析 Fisher、观测响应和噪声框架。当前代码的解析分母包含 P_N，并不是纯 CV；include_Cfg=False 也不等于采用了完整的前景避让模型。

对有效恢复模型 q=g s+e、〈es*〉=0，标准功率互相关系数为

$$
\rho=\frac{P_{qs}}{\sqrt{P_{qq}P_{ss}}},\qquad
N_{\rm rec}=\frac{P_{ee}}{g^2}=P_{ss}(\rho^{-2}-1).
$$

如果响应与噪声偏置已知、残差近似高斯、忽略箱间相关，并采用相应固定响应/噪声的参数求导约定，则

$$
\frac{C_{\rm rec}-C_{\rm CV}}{C_{\rm CV}}=\rho^{-4}-1.
$$

实际原始 mock 在 z=1.501、k<0.025 h/Mpc 上给出 ρ≈0.94–0.99，中位数≈0.983；上述模型估计额外方差/CV 约 0.04–0.28，典型值约 0.07。均一恢复率近似下 σ_rec/σ_CV≈ρ^-2，典型误差退化约 3.5%。这说明低噪声恢复接近理想在物理上可能，但不是完整七参数预报。

这里的标准 ρ 来自归一化一致的互功率和自功率；不能直接用当前 rk_2d_bin_avg 替代，后者在源程序中是逐 Fourier 点的相位相关量再平均。固定一个宇宙场估计的 ρ，也不能替代独立宇宙实现上的完整协方差验证。

更详细的两论文推导、噪声对照与图见 [physical_interpretation.md](./physical_interpretation.md)，数值见 [physical_estimate.json](./physical_estimate.json)。

## 11. 正确比较：先定义 mock 项，再区分两个科学问题

### 11.1 问题 A：固定数据与参数响应，只比较协方差模型

这是直接回答“解析方差与解析加额外方差为何不同”的受控比较。定义同一数据向量 d，例如同一有效域、同一估计器得到的分箱功率；令

$$
\mu(\theta)=\mathbb E[d|\theta],\quad
J_{ja}=\frac{\partial\mu_j}{\partial\theta_a}.
$$

在固定 fiducial 协方差的 Fisher 近似下，

$$
F_A=J^\mathsf{T}C_A^{-1}J,\quad
F_B=J^\mathsf{T}(C_A+C_{\rm extra})^{-1}J,
$$

$$
\sigma_A(f_{\rm NL})=\sqrt{(F_A^{-1})_{ff}},\quad
\sigma_B(f_{\rm NL})=\sqrt{(F_B^{-1})_{ff}}.
$$

两组须保持一致：数据估计器、分箱、有效模式、体积、参数集、fiducial 宇宙学、偏导、nuisance 参数和先验。C_extra 必须是该数据向量中尚未计入 C_A 的贡献，并且已经处理所需交叉项。

对于原始 HI 的无前景 Gaussian 分箱功率，若箱内 P、P_N 变化足够缓慢，可以采用

$$
C_{A,jj}=\frac{2(P_j+P_{N,j})^2}{N_j^{\rm survey}}.
$$

N_j 约定计入实场正负共轭 Fourier 点，与原 Neff_bin 的计数口径一致。若 P_N/窗口在箱内变化明显，应对每个模式的方差按估计器权重累加，不能简单用“平均功率的平方”替代“平方后的平均”。

**采用分箱协方差后，F=J^T C^-1 J 已包含模式数和体积效应，不得再额外乘一次旧积分的 V k³/(8π²) 权重。** 如果坚持连续积分近似，才在有依据的 1/N 缩放下，将 f_sim 转成 (N_sim/2) f_sim 作为分母系数。

### 11.2 三种 mock 不能混用

| mock 的实际含义 | 可以采用的组合 | 需要补充的内容 |
|---|---|---|
| 已验证、原解析模型未包含的独立额外误差协方差 | C_B=C_A+C_extra | 同一估计器与单位；有交叉项时必须加入 |
| 固定信号，只改变噪声的条件协方差 | 全协方差公式中的条件噪声部分 | 不同信号实现的响应散布，以及对信号实现的平均 |
| 独立宇宙实现经过完整管线得到的总协方差 | 直接用 C_B=C_mock,total，或验证过的协方差估计模型 | 不再把完整 C_A 加一次；处理有限模拟样本误差 |

传递函数标定误差还应单独区分：它可能在多个 k 箱间共享，适合通过标定参数及其先验传播，或使用相应相关协方差，不能默认按 Fourier 模式数独立平均。

当前这批 mock 属于第二类。完整协方差满足

$$
\mathrm{Cov}_{S,n}(d_{\rm rec})
=\mathbb E_S[\mathrm{Cov}_n(d_{\rm rec}|S)]
+\mathrm{Cov}_S[\mathbb E_n(d_{\rm rec}|S)].
$$

第一项包含重建后传播的热噪声；第二项包含不同宇宙结构下的平均重建结果的散布。用原始真实场的 2P²/N 代替第二项，需要验证响应、偏置及其宇宙学依赖，现有一个 S 不能完成这种验证。也不能把“改变噪声的 200 个实现”称为“200 个独立宇宙实现”。

如果试图从成对模拟定义残差 r=d_rec-d_ref，则

$$
\mathrm{Cov}(d_{\rm rec})
=\mathrm{Cov}(d_{\rm ref})+\mathrm{Cov}(r)
+\mathrm{Cov}(d_{\rm ref},r)+\mathrm{Cov}(r,d_{\rm ref}).
$$

因此，仅把残差样本协方差加到参考协方差上，仍需检验交叉项。若直接定义 ΔC=C_mock,total-C_A，ΔC 也不保证半正定；不能为了让误差变大而随意截掉其负特征值。应优先估计和验证正定的总协方差模型。

若 C_extra 确实半正定、J 和先验不变，则加它不会改善参数约束。这个性质可以作为受控比较的数值检查，不能据此要求任何“重建与未重建”的物理比较都必须退化。

### 11.3 问题 B：前景避让后，重建恢复了多少 f_NL 约束

这是结合两篇论文的实际科学问题，应至少输出三条结果：

| 结果名称 | 数据与处理 | 均值/偏导 | 协方差 |
|---|---|---|---|
| ideal_no_fg | 无前景、真实仪器响应下可访问的信息 | 对应观测估计器的 μ_ideal、J_ideal | 含仪器噪声的 C_ideal |
| fg_avoidance | 同一仪器，删除前景污染模式 | 保留数据的 μ_clean、J_clean | 保留模式的 C_clean |
| fg_plus_reconstruction | 前景避让后恢复的数据，必要时与保留数据联合 | 完整管线后的 μ_rec 或联合 μ、J | 完整 C_rec 或联合 C |

重建收益可用 σ_FG/σ_FG+rec 表示；与无前景理想状态的差距可用 σ_FG+rec/σ_ideal 表示。同一仪器、观测时间、天区、频率范围和参数假设要贯穿三组；允许的数据处理和响应差异应明确记录。

三组不必共用原始 HI 的 J。正值化会改变振幅和参数响应；同一 f_NL 偏导不能未经验证直接复制到恢复场。

只有对同一恢复数据向量，另行比较其 Gaussian 解析协方差 C_G,rec 和模拟得到的总 C_rec，才是在恢复场上进行严格的“只换协方差”实验。此时可报告 F_rec,Gaussian 与 F_rec,full；如确有经过验证的额外项，也可命名为 F_ana+extra，并注明数据向量是恢复场。

一个已完成的示例：保持旧 SKA 干涉模型其余条件，只保留 |k_parallel|>max(0.2h_sim,0.3k_perp)，σ(f_NL) 从 12.2064 变为 645.8054。这是前景掩码敏感度，不是校准后的真实 SKA 预测。它说明原“无前景解析加微小散布”的结果不能直接度量恢复收益。

### 11.4 联合数据必须保留相关性

如果 d=(P_clean,P_rec)，必须构造

$$
C_{\rm joint}=
\begin{pmatrix}
C_{\rm clean}&C_{\rm clean,rec}\\
C_{\rm rec,clean}&C_{\rm rec}
\end{pmatrix}.
$$

恢复模来自保留的小尺度数据，不是新的独立天空观测。直接将两套 Fisher 相加会在忽略相关性的情况下重复计数信息。只分析恢复的大尺度数据可简化第一阶段建模，但以后加入原始小尺度统计量时仍须联合处理。

同样，不能把“无前景 Gaussian 功率谱 Fisher”自动解释成完整场信息的严格上限：非线性处理可能使用该简单二点统计中未包含的信息。讨论信息上限时，必须比较同一原始信息集、相同参数/先验与一致的统计模型。

## 12. 逐文件代码修改方案

以下是可实施的方案，尚未修改源文件。建议先完成 §13 的受控对照，再实现实际恢复预报。

### 12.1 experiments.py：作为仪器与巡天配置来源

主要位置：[SURVEY](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/experiments.py:49)、[SKA1MIDbase1](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/experiments.py:501)、[HIRAX](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/experiments.py:1167)。

1. 提供解析和 mock 共同读取的 resolved 配置，包括 mode、Ndish、Nbeam、Ddish、Tinst、effic、npol、dnu、Sarea、ttot 和基线文件。记录单位：温度 mK、频宽 MHz、面积 sr；ttot 在接口入口由小时转换一次到现有 baofisher 约定。
2. 显式设置标准配置实际采用的 npol=2、effic=0.7，避免依赖 Cnoise 内部默认值而未写入模拟元数据。
3. 保留 instrument 参数与 simulation/reconstruction 参数的职责分离；mock 文件路径、随机种子、重建阈值放入独立配置，不通过修改 Tinst 等仪器参数来迁就旧 mock。
4. 对 n(x) 文件记录路径与哈希。实际预报遇到缺失文件应明确失败；只有选择了 uniform 基线近似的配置才启用该近似，并在结果中标记。不能让 HIRAX 缺文件后的替代模型与文件模型混为一谈。
5. Cnoise、模拟生成和辅助图表都使用 resolved 后的同一个观测时间。experiments.SURVEY 默认 10000 h，主 notebook 覆盖为 100000 h，最后辅助单元又用 10000 h；选择哪一套属于预报假设，不能隐式混用。
6. CVlimited_z0to3 当前仍含有限 Ndish、Tinst 和基线文件，名称本身不保证 P_N=0。若需纯 CV 对照，新增明确的 thermal_noise=False 设置，在既定观测域内将噪声关闭；不要凭名称认定宇宙方差极限，也不要把 Cnoise 的 cv=True 当作已实现 P_N=0。
7. 使用统一的 fiducial 宇宙学配置并记录来源。experiments.cosmo、notebook 的 CLASS 设置和模拟的 h/Ω_m 并非完全相同；传输模拟响应到另一宇宙学需要验证，不能只修 h 单位便认为物理模型已一致。

### 12.2 baofisher_k.py：分离热噪声、仪器覆盖和分析截止

主要位置：load_interferom_file、interferometer_response、dish_response、Cnoise。

建议提供显式接口，例如：

~~~python
def evaluate_noise_and_support(kper, kpar, z, expt, *, response_policy):
    """待实现：返回 P_noise、instrument_mask、beam、noise_convention。"""
    ...
~~~

1. 热噪声计算保留论文的有效面积、偏振、波束、基线密度与时间因子。
2. load_interferom_file 的物理分支在无基线处返回零密度/无覆盖标记，由调用方赋予不可观测状态；不要因分析 k_min 改变而填基线边缘值。
3. 将 instrumental_uv_support、前景 mask、输出分析 k 区间拆开。KMIN_EFF/KMAX_EFF 不应再同时关闭文件边界、Dmin/FOV、前景频率截止和非线性截止。保留 legacy 分支用于复现旧表，物理预报使用显式 policy。
4. 区分 k_input_max 与 k_output_max：前者是重建可使用的小尺度输入，后者是最终宇宙学统计量的分析范围。不要在生成重建噪声时机械应用输出 Gaussian 理论的非线性截止，否则可能删掉正值化所需的小尺度信息。
5. 默认单视场 FOV 近似与 mosaicing 模型也应是明确选项。简单把两个全局 KMIN_EFF/KMAX_EFF 设回零，不构成完整修复。
6. 为避免 INF_NOISE 的溢出、0×∞ 等数值问题，返回显式 mask；只在有效域上求噪声和构造模拟。
7. 不再用 noise_rms_per_voxel_interferom 给物理 mock 设噪声。若需要体素输出，新增接受共同 W 与 mode mask 的 P_N 积分函数；旧辅助函数标为 legacy 诊断。

必须明确噪声表示的约定。现有 P_N=r²rnu Cnoise 是波束反卷积后的等效噪声；若模拟在观测图上先卷积信号 B s，就应使用 P_N,obs=|B|²P_N,deconv，而不能同时把完整反卷积噪声直接加到 B s 上。也可使用明确定义的反卷积表示，但其有效域和重建管线必须保持一致。

### 12.3 Tutorial notebook：修正统计对象和导出格式

主要修改第 4 个单元的 avg_pkl_list，以及第 8–9 个单元的统计/导出（按人类从 1 开始计数）。

1. 删除逐 ndarray 的端点平均。按实际 z、noise_std、seed_ids、wdg、bin_edges、仪器配置键保存各组，不依赖排序后的 [7,15,…] 隐式含义。
2. 在每个噪声档位上独立计算 mean、ddof=1 的 variance/covariance。200 个噪声实现用于估计一次巡天统计量的散布，不再额外除以 200。
3. 导出实际 z、h_sim、box_size、grid_shape、k/power/covariance 单位、bin_edges、N_sim_modes、seed_ids 和噪声模型；用字段 covariance_kind 明确该文件是 conditional_noise_fixed_signal、total 或 verified_extra。
4. 将 fisher_arr 更名为 relative_bandpower_variance 或类似明确名称，它本身不是 Fisher 信息。旧文件仅通过显式 legacy loader 读取。
5. 除对角 f 外，至少保留选定数据向量的 covariance、mean_power、cross_power 和响应信息。标准互相关系数应由同归一化的功率计算；不能直接复用逐模平均的 rk_bin。
6. 传递函数校准集、参数偏导模拟和最终验证集应分开。校准算子在 fiducial 处拟合后固定，不能对每个测试宇宙重新用该宇宙真实 P 来强制恢复平均功率，再以此证明参数响应正确。

建议的新文件数据契约：

~~~text
schema_version
covariance_kind, covariance_estimator, estimator_definition
z_actual, fiducial_cosmology, h_sim
k_unit, power_unit, covariance_unit
box_size, box_unit, grid_shape, voxel_volume
kper_edges, kpar_edges, selected_bin_ids, N_sim_modes
instrument_config, noise_model, noise_convention
noise_rms_or_amplitude, input_mode_mask, foreground_mask
reconstruction_settings, response_calibration_id
n_signal_realizations, n_noise_per_signal, seed_ids
mean_power, covariance, response, cross_power, support_mask
~~~

对于相同仪器、相同谱形的噪声，可以定义振幅轴 a_N，使 P_N(a_N)=a_N²P_N,ref，并在模拟网格上标定响应与协方差。当前 std 是 mK 的绝对 RMS，不能直接改名为倍数。

若只是在已有两档之间插值：对同一定义、同一分箱的均值与协方差作为参数函数分别插值，并用保留的中间档位验证。协方差采用非负且和为 1 的权重组合，可保持半正定性。不要把不同档位的样本平均或混合后当成目标噪声样本。目标噪声超出现有范围，或谱形因仪器变化而不同，需要新增匹配模拟；不能靠外推 scalar f 完成。

### 12.4 6.7 notebook：统一 Fisher 核心与两套比较

修改入口：SurveyConfig.load、P_noise_grid、fisher_sum_analytic、fisher_sum_mock_zinterp、sigma_from_fisher、run_forecast 和绘图/导出。

1. 在 SurveyConfig 中显式记录 scenario、noise convention、实际 survey 时间、foreground model、k_input/output 范围、mock support policy、参数和先验；从同一 resolved 仪器配置构建所有分支。
2. 修正 expt_to_zmin_zmax 的物理频段换算。当前使用 -1.1028 和 -0.8083；实际应由 z=ν21/ν-1 得到。SKA 1050–350 MHz 对应约 z=0.3528–3.0583，旧代码人为扩到约 0.2500–3.2500。需要自定义 z 分箱时，将其与真实频率覆盖求交，再按所选数据域重新计算体积。
3. 读取新版本 mock 元数据；在加载时统一成 Mpc、Mpc^-1、mK² Mpc³ 和相应协方差单位。分析代码中不再猜测 mock 红移或单位。
4. mock 未覆盖处返回 missing/support=False，不再 fill_value=0 或对 z 无条件 clip。受控协方差比较使用双方共同的有效域，并对两组同时应用；物理重建预报只纳入经过校准的恢复模式。未经过重建的干净观测可按其自身解析模型进入联合数据向量。
5. 两组 covariance-only 计算调用同一个 fisher_from_bandpowers(J,C)；不再复制两份求和逻辑。协方差已含模式数时，删除额外几何计数。
6. 物理三组分别构造 mean_and_jacobian(scenario)、covariance(scenario)，保证响应差异来自真实处理过程。只有受控诊断才刻意固定原 J_HI。
7. sigma_from_fisher 使用参数缩放后的正定性/秩检查与线性求解。不再用 abs(diag) 掩盖负方差，也不通过自动 pinv 将不可约束方向输出为虚假的有限甚至零误差。
8. 先输出 HI-only，再对所有对应分支加入同一、参数顺序一致的 CMB 等先验。不能用强先验后的近似相等代替 HI 信息恢复检验。
9. 输出文件写入 estimator、covariance_kind、scenario、单位、t_obs、频段、有效域、协方差样本数和版本。将 legacy、normalization_diagnostic 与 physical_forecast 分开命名。
10. compute_realspace_variance_table 使用真正对应的 P_N 和窗口，并标明热噪声方差与信号+噪声总方差，避免把所有模式都画成单碟体素噪声。

除四个用户文件外，实际噪声匹配还涉及 [direct_noise_to_prod_dict.py](/data/kcchan_rec/ding_post_mess/jupyter_mess/aureum/fisher_mess/sbatch_scripts/codex_gen/direct_noise_to_prod_dict.py)。只修改 6.7 的插值器，无法把已有白噪声 mock 变成各仪器的各向异性噪声模拟。

## 13. 核心实现示例与必要校验

### 13.1 现有数据可先完成的归一化对照

以下只展示一个原始噪声档位的统计。N_sim_modes 不是噪声实现数：

~~~python
import numpy as np

def relative_variance_one_noise_level(transfer_samples):
    """输入 [n_noise, n_kper, n_kpar]；输出固定信号的条件相对散布。"""
    samples = np.asarray(transfer_samples, dtype=np.float64)
    if samples.ndim != 3 or samples.shape[0] < 2:
        raise ValueError("Need at least two noise realizations per grid.")
    mean = samples.mean(axis=0)
    if not np.all(np.isfinite(samples)) or np.any(mean <= 0):
        raise ValueError("Invalid transfer samples; resolve support explicitly.")
    return samples.var(axis=0, ddof=1) / mean**2

def diagnostic_extra_variance(P, f_sim, n_sim, n_survey):
    """仅用于已声明的 1/N、同单位同分箱运输近似。"""
    return f_sim * P**2 * n_sim / n_survey
~~~

将各个噪声档位分别导出；在单位、分箱和有效域对齐之后，用 C_extra,jj=f_sim,j P_j² N_sim,j/N_survey,j 做受控实验。若同物理分箱下模式数正比于体积，上式比值相当于 V_sim/V_survey。

这种运输不适用于共享的传递函数标定误差，也不能自动处理真实巡天窗口、箱间相关或超样本协方差。对于一般重分箱，先定义共同线性算子 A，按 μ'=Aμ、C'=ACA^T 变换统计量；不能只插值 C 的对角线就声称保留了完整协方差。

这一步可解释旧结果对归一化的敏感性，但输出必须标为 diagnostic。现有固定信号 mock 尚未被证明是原 C_ana 之外的独立额外项，不能仅凭这段代码就宣称得到实际 F_ana+mock。

### 13.2 通用的分箱 Fisher 与边缘化误差

下列核心采用固定 fiducial 协方差和均值偏导，J 的 shape 为 [n_data,n_parameters]。不额外加入可能与功率谱信息重复计数的协方差偏导项。

~~~python
import numpy as np
from scipy.linalg import cho_factor, cho_solve

def fisher_from_bandpowers(J, covariance):
    J = np.asarray(J, dtype=float)
    C = np.asarray(covariance, dtype=float)
    if J.ndim != 2 or C.shape != (J.shape[0], J.shape[0]):
        raise ValueError("Jacobian and covariance dimensions do not match.")
    if not np.all(np.isfinite(J)) or not np.all(np.isfinite(C)):
        raise ValueError("Non-finite data: resolve masks before Fisher assembly.")
    diagonal = np.diag(C)
    if np.any(diagonal <= 0):
        raise ValueError("Covariance must have a positive diagonal.")
    scale = np.sqrt(diagonal)
    correlation = C / np.outer(scale, scale)
    if not np.allclose(correlation, correlation.T, rtol=1e-10, atol=1e-12):
        raise ValueError("Covariance must be symmetric.")
    correlation = 0.5 * (correlation + correlation.T)
    weighted_J = J / scale[:, None]
    factor = cho_factor(correlation, lower=True, check_finite=True)
    F = weighted_J.T @ cho_solve(factor, weighted_J)
    return 0.5 * (F + F.T)

def marginalized_sigma(F, rank_rtol=1e-12):
    F = np.asarray(F, dtype=float)
    if F.ndim != 2 or F.shape[0] != F.shape[1] or not np.all(np.isfinite(F)):
        raise ValueError("Invalid Fisher matrix.")
    if np.any(np.diag(F) <= 0):
        raise ValueError("Unconstrained or invalid parameter direction.")
    scale = np.sqrt(np.diag(F))
    G = F / np.outer(scale, scale)
    if not np.allclose(G, G.T, rtol=1e-10, atol=1e-12):
        raise ValueError("Fisher matrix must be symmetric.")
    G = 0.5 * (G + G.T)
    eig = np.linalg.eigvalsh(G)
    if eig[0] <= rank_rtol * eig[-1]:
        raise ValueError("Fisher is singular or unstable; inspect degeneracies.")
    cov_scaled = cho_solve(cho_factor(G, lower=True), np.eye(G.shape[0]))
    return np.sqrt(np.diag(cov_scaled)) / scale
~~~

调用方式为 F_A=fisher_from_bandpowers(J,C_A)、F_B=fisher_from_bandpowers(J,C_A+C_extra)。如有相同外部先验，在计算 marginalized_sigma 之前分别加入。若 mock 是总协方差，则第二个参数直接传 C_mock,total。

这是已知协方差模型下的数值核心，不自动校正有限模拟样本造成的精度矩阵偏差。出现奇异矩阵时，应分析哪些方向不可约束、是否需要有依据的先验或事先定义的数据压缩；不能靠 abs 或任意 jitter 制造约束。

### 13.3 真正的 f_NL 响应必须经过重建管线

优先用成对相位、成对噪声的参数扰动模拟估计

$$
J_{{\rm rec},j f}
\simeq
\frac{\langle d_{{\rm rec},j}\rangle_{f_0+\Delta f}
-\langle d_{{\rm rec},j}\rangle_{f_0-\Delta f}}
{2\Delta f}.
$$

不同参数点使用相同的 fiducial 校准算子、掩码和重建设置；模拟校准/检验样本独立。使用至少两个 Δf 检查有限差分稳定性。其他宇宙学及必要的 HI 偏置、温度或传递函数 nuisance 参数采用一致方案。

如果在未校准场上建模，

$$
\mu_{\rm rec}=g^2(\theta)P(\theta)+P_e(\theta),
$$

则 ∂μ_rec/∂f_NL 包括 g²∂P/∂f_NL、2gP∂g/∂f_NL 和 ∂P_e/∂f_NL。只有经过验证这些额外依赖可忽略，或已经明确建模，才能简化为原始功率偏导乘固定响应。

目前用同一套实现的平均恢复功率除以同一个真实功率得到传递函数，会使平均恢复功率回到真实值成为构造恒等式。必须在测试宇宙上验证，而不能将这个恒等式当作无偏和正确 f_NL 响应的证据。BAO 波纹/相位的恢复演示不能替代振幅型 f_NL 响应验证。

### 13.4 协方差样本数与维度

200 个实现不能直接估计并求逆 40000 维的经验协方差；去均值后的样本协方差秩最多为 199。即使维度小于实现数，精度矩阵也可能有明显估计偏差。[Hartlap et al.](https://arxiv.org/abs/astro-ph/0608064)

建议先用少量物理上明确的大尺度分箱或经验证的数据压缩，保留 f_NL 的尺度依赖，再增加独立宇宙实现并验证协方差和 σ 收敛。协方差 shrinkage/压缩需要在独立样本上验证。

Hartlap 类修正依赖独立 Gaussian 数据向量等假设；不能把它机械用于正值化后的非高斯数据，或把同一信号下的噪声重复当成完整宇宙样本。进入实际参数推断时，还应处理模拟估计协方差自身的不确定性；Gaussian/Wishart 假设下可参考 [Sellentin & Heavens](https://arxiv.org/abs/1511.05969)。在拟合收缩协方差后，也不能自动套用未经推导的样本协方差修正因子。

## 14. 推荐实施顺序、输出与验收

### 第一阶段：让当前两组数值可解释

1. 建立显式元数据和单位读取，分别统计原始噪声档位。
2. 用同一个 J、同一共同有效域、同一先验，计算解析与“声明假设下额外项”的分箱 Fisher。
3. 同时输出原 legacy 结果、只改归一化、再改单位/红移、再统一有效域的逐步对照。每次变更有效域，都重新计算对应解析基准。
4. 输出 R 的分位数、mock 未覆盖的参数灵敏度权重、矩阵条件情况、σ(f_NL) 与相对变化；完整协方差时还输出箱间相关。

这一阶段用现有数据即可完成，用途是解释原代码。不能将缺失的宇宙实现或仪器校准信息用常数倍率补齐。

### 第二阶段：建立实际前景与重建预报

1. 统一 resolved 仪器/巡天配置；用真实覆盖、波束和 P_N 生成各仪器匹配的噪声模拟，分开仪器、前景和分析截止。
2. 生成独立信号实现、热噪声实现及参数扰动模拟；固定独立标定的重建处理。
3. 估计 μ_rec、J_rec、总协方差及与保留数据的交叉协方差，验证响应、偏置和协方差的体积运输。
4. 输出 ideal_no_fg、fg_avoidance、fg_plus_reconstruction 三组 HI-only 与相同先验下的 σ；另给恢复场 Gaussian/完整协方差的受控比较。

最终结果表至少包括 instrument、scenario、estimator、covariance_kind、t_obs、sky/frequency/k 范围、n_signal/n_noise、n_data、σ_fNL_HI、σ_fNL_HI+CMB，以及相对于 ideal 和 foreground-only 的误差比。

### 验收条件

- C_extra=0 时，受控对照的两组 F 和 σ 一致。
- 同一 J 和有效域、无固定外部先验，C_extra=R C_A 时，σ_B/σ_A=√(1+R)。
- 对真正半正定的独立 C_extra，加项后受控参数约束不会变强。
- k、P、V 和协方差完整地作 h 单位转换后，物理 Fisher 不因单位表示而变化。
- 增加生成的噪声实现数只提高协方差估计精度，不自动使单次巡天误差按 1/√N_real 变小。
- 模拟噪声的测得功率谱与目标 P_N/观测表示匹配；没有把反卷积噪声和波束滤波重复使用。
- mock 缺失域被显式标记，两组受控比较共用有效域；不会把未知恢复误差填成零。
- 检查输入与输出波数范围、基线/频段支持，确保没有人工生成不可观测的大尺度数据，也没有误砍重建所需的小尺度。
- 校准/测试分离，参数有限差分、数据压缩和有限样本协方差对 σ 的影响收敛。

**实施目标是获得可解释、经校准的约束差异；不是保证加入 mock 后 σ 必须明显变化。**

### 本次文档更新的验证范围

2026-09-16 核对了四个科学源文件，复用前次已验证的数值结果；没有重新运行整个 CLASS/mock 生产任务，也没有修改原科学代码或重写既有结果。3 个 Python 示例均通过语法检查；数值核心通过 7 项一致性检查：零额外项、常比例协方差缩放、半正定附加项的误差单调性、与直接求逆交叉验证、功率/协方差单位缩放不变性、奇异方向拒绝和模式数转换。所有文档内本地链接均存在，四个源文件及原 mock 导出文件的 SHA256 与本次开始时一致。这些代数和文档检查不构成对实际重建模型的物理校准。

# mock 方差与 fNL 约束：结论、数值对照和修改方案

更新：2026-09-16。约束表中的数值均为仅使用 HI 数据、七参数边缘化后的 $\sigma(f_{\rm NL})$，即原讨论中的 $\sigma(F_{\rm ana})$ 和 $\sigma(F_{\rm ana+mock})$；$F$ 本身表示 Fisher 矩阵。

## 1. 先回答：修正后会相差多少？

**已有数据支持：归一化、噪声档位处理和坐标修正的对照计算，使各配置的误差增加约 0.7%–13%。这不是五项问题全部解决后的实际观测预测。** 仪器匹配、mock 有效域外的恢复能力、不同宇宙实现的恢复残差，以及前景后的参数响应，仍需要重新标定。

可以把目前的结论分为三层：

| 层次 | 可以给出的量级 | 如何理解 |
|---|---|---|
| 当前代码 | $\Delta\sigma/\sigma\simeq0.002\%$–$0.007\%$ | 额外项远小于原分母，结果几乎不变 |
| 现有 mock 的修正对照 | $\Delta\sigma/\sigma\simeq0.7\%$–$13\%$ | 说明原归一化确实掩盖了影响，但还没有完成物理校准 |
| 真实前景恢复预测 | 高相关恢复可对应几个百分点；较差恢复可对应几十个百分点或倍数差异 | 必须指定恢复率、基准协方差和响应；目前不能给出统一的最终区间 |

**核心原因有两个层次：** 数值上，分箱后的散布被当成单模额外方差，且再次受到信号占比的抑制；物理上，当前“解析 + mock”仍保留理想大尺度响应，只添加固定宇宙场下的部分噪声散布，没有真正描述“前景损失后再恢复”的信息。

## 2. 为什么当前结果几乎不变？

当前分母可以写成

$$
D_{\rm ana}=(P_{\rm HI}+P_N)^2,\qquad
D_{\rm mock}=f_{\rm bin}P_{\rm HI}^2,\qquad
R=\frac{D_{\rm mock}}{D_{\rm ana}}
=f_{\rm bin}\left(\frac{P_{\rm HI}}{P_{\rm HI}+P_N}\right)^2.
$$

这里的 $R$ 是代码所称 `Var_mock/Var_ana`。每个模式的 Fisher 权重乘以 $1/(1+R)$；小扰动时，误差的相对变化约为参数敏感度加权的 $\langle R\rangle/2$，不能直接用所有网格点的中位数代替。

| 原因 | 对当前结果的影响 | 修正原则 |
|---|---|---|
| 分箱与单模归一化混用 | mock 已经平均模式，Fisher 又计入模式数 | 若确实满足独立模式的 $1/N$ 缩放，应使用 $f_{\rm mode}=N_{\rm sim}f_{\rm bin}/2$；系统误差和相关残差不能直接这样转换 |
| 先平均两个噪声档位的实现，再求方差 | 现有样本散布约被压低一半 | 分别估计端点统计量，再标定噪声依赖；这不等于对协方差进行正确插值 |
| mock 未匹配仪器 | 同一批白噪声无法代表各仪器的 $P_N(k,\mu)$；上述信号占比还会压低 $R$ | 匹配体素、时间、波束、基线与各向异性噪声 |
| 单位、红移和有效域 | $h/{\rm Mpc}$ 与 ${\rm Mpc}^{-1}$ 混用；越界填零会人为消除额外误差 | 统一单位与实际红移；使用共同有效域或经验证的外推模型 |
| mock 只改变热噪声 | 没有完整覆盖不同宇宙结构的恢复误差；解析端仍使用受前景污染的大尺度响应 | 使用独立宇宙实现，并联合标定恢复后的均值响应和协方差 |

这里 $N_{\rm sim}$ 是模拟分箱的有效傅里叶模式数，不是 200 个噪声实现的数量；因子 2 采用 $\mathrm{Var}(\widehat P)=2P^2/N$ 的模式计数约定。

`aux_i/d≈3.6e-7` 所对应的辅助量没有进入当前 Fisher 主计算，不能解释主要结果；它也不是具有共同窗口的两种仪器体素方差之比。

## 3. 已有数值：原始结果与修正对照

### 3.1 各配置的约束

以下复用已保存的计算结果，主配置观测时间为 $10^5$ 小时。“修正对照”包含模式数转换、分别处理噪声端点、$k$ 单位与实际红移修正，**仍保留有效域外额外项为零的旧处理，也未完成仪器噪声匹配及完整恢复协方差标定。**

| 配置 | $\sigma_{\rm ana}$ | 原始 $\sigma_{\rm ana+mock}$ | 修正对照 $\sigma$ | 修正对照相对增加 |
|---|---:|---:|---:|---:|
| SKA 干涉 | 12.20640 | 12.20671 | 12.29209 | 0.70% |
| SKA 单碟 | 4.89864 | 4.89884 | 4.98721 | 1.81% |
| CHIME | 8.94098 | 8.94123 | 9.35600 | 4.64% |
| HIRAX | 5.71246 | 5.71258 | 5.80831 | 1.68% |
| Tianlai | 20.35478 | 20.35590 | 23.00349 | 13.01% |
| MeerKAT | 77.24729 | 77.25243 | 78.35243 | 1.43% |
| `cv_z0to3` 配置 | 3.13896 | 3.13904 | 3.19394 | 1.75% |

`cv_z0to3` 是配置名称，不能据此认定它严格满足 $P_N=0$。

### 3.2 ratio 到底有多大？

在 $z\simeq1.5$，仅统计 $R>0$ 的网格点：

| 配置及处理 | median | p90 | p99 | max |
|---|---:|---:|---:|---:|
| SKA 干涉，原始 | $2.183\times10^{-6}$ | $3.339\times10^{-5}$ | $8.884\times10^{-5}$ | $1.012\times10^{-4}$ |
| SKA 干涉，修正对照 | $3.144\times10^{-3}$ | $4.360\times10^{-3}$ | $4.979\times10^{-3}$ | $5.683\times10^{-3}$ |
| SKA 单碟，修正对照 | $6.629\times10^{-4}$ | $2.175\times10^{-2}$ | $3.181\times10^{-2}$ | $6.384\times10^{-2}$ |

坐标修正改变了支持域，原始与修正后的有效点数分别为 3990 和 4201，因此这些不是逐点同域的分位数对比，也不是所有红移、所有仪器的范围。原始 SKA 干涉全红移的参数敏感度加权 $\langle R\rangle\simeq5.208\times10^{-5}$，对应 $\Delta\sigma/\sigma\simeq2.604\times10^{-5}$，与结果一致。

若整个附加协方差满足 $C_{\rm extra}=R C_{\rm ana}$、$R$ 为常数，且响应、有效域不变、没有固定的外部先验，则

$$
\frac{\sigma_{\rm ana+extra}}{\sigma_{\rm ana}}=\sqrt{1+R}.
$$

例如 $R=0.1,\ 0.5,\ 1,\ 3$ 分别对应误差增加 $4.9\%,\ 22.5\%,\ 41.4\%,\ 100\%$。这只是换算关系；非均匀 $R$ 需要重新积分和边缘化。

## 4. 五项都修正后：可以做怎样的物理预估？

### 4.1 用恢复相关系数建立条件估计

设恢复场 $q=gs+e$，残差与真实信号不相关。由交叉谱定义相关系数及除去响应 $g$ 后的等效残差噪声：

$$
\rho=\frac{P_{qs}}{\sqrt{P_{qq}P_{ss}}},\qquad
N_{\rm rec}=P_{ss}(\rho^{-2}-1).
$$

在响应 $g$ 与噪声偏置已知、残差与模式满足高斯独立近似时，相对于**无热噪声的宇宙方差基准**，有

$$
R_{\rm extra/CV}=\frac{C_{\rm rec}-C_{\rm CV}}{C_{\rm CV}}
=\rho^{-4}-1,\qquad
\frac{\sigma_{\rm rec}}{\sigma_{\rm CV}}\simeq\rho^{-2}.
$$

后一式还要求相关系数及信息效率近似均匀，且没有额外响应退化。

| 假定恢复相关系数 $\rho$ | $R_{\rm extra/CV}$ | 理想化误差增加 |
|---:|---:|---:|
| 0.99 | 0.041 | 2.0% |
| 0.983 | 0.071 | 3.5% |
| 0.95 | 0.228 | 10.8% |
| 0.90 | 0.524 | 23.5% |
| 0.80 | 1.441 | 56.3% |
| 0.70 | 3.165 | 104.1% |

现有固定宇宙场 mock 在 $z\simeq1.501$、$k<0.025\,h/{\rm Mpc}$ 的低 $k$ 分箱中，两个噪声端点的 $\rho$ 中位数分别约为 0.98286 和 0.98221，对应 $R_{\rm extra/CV}$ 中位数约 0.0716 和 0.0745。六个红移的低 $k$ 分箱总体约为 $0.03$–$0.575$，各红移中位数约为 $0.066$–$0.161$，在上述均匀效率近似下对应约 **3%–8%** 的误差增加。

**这些是条件预估，不是独立宇宙实现下测得的完整协方差，也不是已经计算出的七参数约束。** 恢复论文附录 B、图 7 中约 $\rho=0.9$ 至 $0.7$ 的噪声依赖示例针对特定 $k_{\rm cut}=0.5\,h/{\rm Mpc}$ 设置，不能直接推广到所有低 $k$ 模式和望远镜。上表的较低相关系数应作为压力情景。

### 4.2 不要混淆两个 ratio，也不要重复加热噪声

上一节的 $R_{\rm extra/CV}$ 不等于代码中含热噪声基准的 $R$。若 $N_{\rm rec}$ 已含热噪声传播，则在同一信号约定下

$$
\frac{C_{\rm rec}}{C_{\rm ana}}
=\left(\frac{P+N_{\rm rec}}{P+P_N}\right)^2
=\frac{\rho^{-4}}{(1+P_N/P)^2}.
$$

不能再把原来的 $P_N$ 重复加进 $N_{\rm rec}$。当前的条件噪声散布也不能直接当成完整的 $C_{\rm rec}$。因此，**不能把“0.03–0.575”宣称为五项修正后代码 ratio 的最终区间**；现有资料不足以给出这种统一区间，恢复率很低时误差损失还可以更大。

前景响应的重要性可由一个诊断看出：仅在原 SKA 干涉模型中施加

$$
|k_\parallel|>\max(0.2h_{\rm sim},\,0.3k_\perp),\qquad h_{\rm sim}=0.6766,
$$

其中波数均以 ${\rm Mpc}^{-1}$ 表示，误差从 12.21 增至约 645.81。这只是保留其他旧设定的掩膜实验，不是校准后的真实预测；但它说明，前景是否移除关键大尺度响应，比当前极小额外方差更能改变结论。

## 5. 同一体素：干涉与单碟方差能差多少？

### 5.1 必须规定共同空间窗口

体素热噪声方差应定义为

$$
\sigma_{N,W}^2=\int\frac{d^3k}{(2\pi)^3}\,P_N(\mathbf{k})|W(\mathbf{k})|^2.
$$

同一像素边长并不保证同一个 $W$：波束、基线覆盖和滤波都影响它。`baofisher_k.py` 的主要比较对象是傅里叶协方差/噪声功率，需转换到统一功率和波束约定，再按上式计算体素方差。

使用当前 SKA 配置及其基线文件，在 $z\simeq1.5$、相同观测时间下，选取共同可观测的横向波数带（带内单位传递、带外为零），并对二者使用相同径向窗口，得到下列**带通图体素热噪声**示例：

| 共同 $k_\perp$ 窗口（${\rm Mpc}^{-1}$） | $\sigma_{N,i}^2/\sigma_{N,d}^2$ | 干涉热噪声方差低多少 |
|---|---:|---:|
| 0.04–0.05 | 0.4931 | 约 2.0 倍 |
| 0.05–0.06 | 0.04551 | 约 22.0 倍 |
| 0.04–0.06 | 0.07851 | 约 12.7 倍 |

这是明确窗口下的估计，**不是所有原生体素通用的倍数**。最后一行对应 RMS 比约 0.280，而不是 0.0785。单个傅里叶模式上，在 $k_\perp=0.04,0.05,0.06\,{\rm Mpc}^{-1}$ 处，噪声功率比分别约为 1.666、0.205、0.0166；比值随尺度变化很快。

当前干涉视场近似给出的下限约为 $k_\perp=0.03976\,{\rm Mpc}^{-1}$。更低波数不能通过边界填充值当作真实覆盖；具体镶嵌观测还需另建响应模型。因此上述带通比较也不能证明干涉仪可以直接观测全部大尺度信息。

若比较包含共同信号的总场方差，应使用

$$
\frac{\sigma_{S,W}^2+\sigma_{N,i,W}^2}
{\sigma_{S,W}^2+\sigma_{N,d,W}^2},
$$

信号占优时该比值趋近 1。它与功率谱估计量方差又是不同的统计量。

### 5.2 当前 mock 噪声为何不能直接套用？

当前配置在 $z\simeq1.5$、通道宽度 0.1 MHz 下，单碟原生波束体素约为 $2.065\times10^4\,{\rm Mpc}^3$；模拟网格体素约为 $649.5\,{\rm Mpc}^3$。把原生体素 RMS 直接赋给较小模拟像素，会改变白噪声功率 $P_N=\sigma_{\rm pix}^2V_{\rm pix}$。

| 噪声定义 | $P_N$（${\rm mK}^2\,{\rm Mpc}^3$） |
|---|---:|
| 当前 mock 白噪声，RMS 约 0.1192 mK | 9.23 |
| 当前单碟配置，$10^4$ 小时，未做波束反卷积 | 485.6 |
| 当前单碟配置，$10^5$ 小时，未做波束反卷积 | 48.56 |
| 当前干涉配置，$10^5$ 小时，$k_\perp\simeq0.04$–$0.2\,{\rm Mpc}^{-1}$ 的代表性噪声 | 约 $2.7\times10^3$–$3.8\times10^3$ |

这些是用于说明噪声匹配的不同功率基准，不是上一表共同窗口的方差比。当前 mock 的白噪声功率比主配置单碟的未反卷积基准低约 5.3 倍，比所列干涉模式低约 300–400 倍。恢复是非线性过程，**不能按这些倍数直接放大最终 mock 方差**；需要按匹配的噪声谱重新运行恢复。

## 6. 正确的比较与最小修改方案

### 6.1 两类问题分别计算

**检查附加误差的影响：** 使用完全相同的数据定义、分箱、支持域、体积、响应 $J$ 和先验，比较

$$
F_A=J^TC_A^{-1}J,\qquad
F_B=J^T(C_A+C_{\rm extra})^{-1}J.
$$

只有确属基准未包含的额外协方差才可相加。若 mock 给的是总协方差，直接使用它；若两个误差相关，应保留交叉项。模式数已经包含在分箱协方差中时，不要在 Fisher 外再次乘模式数/体积。

**预测前景恢复的实际效果：** 分别计算 `ideal_no_fg`、`fg_avoidance` 和 `fg_plus_reconstruction`。第三种必须使用恢复后数据的 $J_{\rm rec}$ 与 $C_{\rm rec}$，通过参数扰动标定 $f_{\rm NL}$ 响应；不能只保留理想响应并增加方差。联合未污染数据与恢复数据时，应包含它们的交叉协方差。

当前模拟只能测一个宇宙场 $S$ 下的条件噪声散布，不能估计下式的完整两项：

$$
\mathrm{Cov}_{S,n}(d_{\rm rec})
=\mathbb E_S[\mathrm{Cov}_n(d_{\rm rec}\mid S)]
+\mathrm{Cov}_S[\mathbb E_n(d_{\rm rec}\mid S)].
$$

恢复率本身也不足以代替对 $f_{\rm NL}$ 响应的测量。

### 6.2 文件修改方案

| 文件 | 必要修改 |
|---|---|
| `baofisher_k.py` | 分开定义热噪声、真实基线支持域和前景掩膜；统一波束与功率单位，保留不可观测模式的状态。体素比较使用共同窗口积分，停止使用 `aux_i/d` 解释物理方差比。 |
| Tutorial notebook 及上游模拟生成器 | 独立统计噪声端点；输出实际红移、单位、模式数、噪声谱、协方差类型。加入独立宇宙场及参数扰动，用匹配仪器的噪声重新生成和校准恢复数据。 |
| 6.7 notebook | 用共同分箱的协方差 Fisher 接口；消除未知域填零和重复模式计数。分别输出三种前景场景的响应、协方差和边缘化误差。 |

先用现有数据完成共同有效域上的归一化对照，再生产新的仪器匹配 mock。不能仅靠对旧方差数组乘一个常数，完成五项修正。

实现时用线性求解代替显式求逆；拒绝奇异约束方向，不用取绝对值或伪逆把不可约束方向变成有限误差。模拟数量有限时先进行合理的数据压缩，再检查协方差估计与逆协方差偏差。

## 7. 数据与复核说明

本次为报告整理与已有结果核对，没有重新运行完整 CLASS/mock 生产任务，也没有修改四个科学源文件。数值来自此前保存的审计产物；第 4 节是带假设的物理预估，第 5 节是当前配置下指定窗口的比较。它们都不代表已经完成真实仪器与重建响应校准。

- [数值结果 JSON](physical_estimate.json)、[原始约束审计](forecast_audit.json)、[原始 mock 审计](raw_mock_audit.json)。
- [数值估算脚本](physical_estimate.py)、[约束复现脚本](reproduce_forecasts.py)。
- [两篇论文的详细物理解读](physical_interpretation.md)。
- [精简前完整报告归档](analysis_detailed_archive_20260916.md)：保留历史讨论、源码定位、详细推导及代码示例，供追溯。
- 恢复方法：[Recovering Cosmic Structure with a Simple Physical Constraint](https://arxiv.org/html/2503.20434v1)，尤其噪声与恢复率讨论。
- 解析框架：[Late-time Cosmology with 21cm Intensity Mapping Experiments](https://arxiv.org/abs/1405.1452)，用于信号、噪声和 Fisher 建模；是否忽略前景取决于实际采用的配置，不能仅按论文名称认定。

## 8. 为什么 aux_i/d≈3.6e-7

[baofisher_k.py 的单碟辅助函数](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/baofisher_k.py:1598) 实现

\[
\sigma_{d,\mathrm{aux}}^2
=\frac{T_{\rm sys}^2 S_{\rm area}}{\Delta\nu\,t\,\theta^2N_dN_b}.
\]

[干涉辅助函数](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/baofisher_k.py:1618) 实际实现

\[
\sigma_{i,\mathrm{aux}}^2
=\frac{T_{\rm sys}^2 S_{\rm area}}{\Delta\nu\,t}\,\mathrm{FOV}^2,
\quad \mathrm{FOV}=\theta^2,\quad\theta=\lambda/D.
\]

所以不需要任何物理猜测就能得到

\[
\frac{\sigma_{i,\mathrm{aux}}^2}{\sigma_{d,\mathrm{aux}}^2}
=N_dN_b\theta^6.
\]

SKA 的 z=1.5、D=15 m、Nd=190、Nb=1 给出 θ=0.0352012，故比值为 `3.61492e-7`，完全复现。

**这个值不是可信的同体素灵敏度比。** 干涉函数的 docstring 写了 n(u)，实现没有 n(u)，也没有接收 u/k 或成像权重；更换基线分布不会改变返回值。不能仅凭函数名将其解释为完整干涉图像体素的方差，或把整个视场平均作为解释。

### Fisher 实际使用的噪声

主程序调用 [Cnoise](/home/liuhk/bao21cm-master/bao21cm-master/radiofisher/baofisher_k.py:1739)，经 `P_N=r²*rnu*Cnoise` 转成 mK² Mpc³。标准碟阵干涉分支为

\[
C_N=\frac{T_{\rm sys}^2S_{\rm area}}{n_{\rm pol}t\nu_{21}}
\frac{\lambda^4}{N_b(A_{\rm eff}\theta)^2}
\frac{e^{E_\parallel}}{n(u)}.
\]

同一标准碟阵仅切换 dish/interferom 时，共同因子消去，得到

\[
\frac{P_{N,i}}{P_{N,d}}=
\frac{N_d\theta^2}{n(u)}\,e^{-E_\perp},
\qquad E_\perp=(k_\perp/\sigma_{k\perp})^2.
\]

这里明确依赖基线密度和尺度。在 notebook 的 z≈1.5、k≈0.0493、mu≈0.495 上，比例约 0.96，和 3.6e-7 完全不同。高红移、高 k_perp 时单碟噪声由于波束反卷积指数增长，干涉可能占优；大尺度的比较则由短基线覆盖决定。

### 体素方差应怎样定义

对于指定的地图平滑窗口 W 和可观测模式集合，定义

\[
\sigma_{N,W}^2=\int\frac{d^3k}{(2\pi)^3}P_N(\mathbf k)|W(\mathbf k)|^2.
\]

不能在没有窗口、分辨率和模式选择的情况下给出唯一的干涉“体素方差”。缺测模式须通过明确的观测算子处理，不能简单把无穷噪声当成零。

对近似白噪声的单碟地图，`P_N≈sigma_vox²*V_vox`，其中 `V_vox≈(r*theta)²*rnu*dnu/nu21`。单碟辅助函数还省略了完整 Cnoise 中极化与有效面积系数；在当前 npol=2、effic=0.7 的标准碟阵下，该系数为 1.6542。因此辅助函数和 Cnoise 也不能在忽略这些约定时逐项等同。

[6.7 的方差表](/home/liuhk/research/6.7-interferometer-telescopes-2.ipynb:2233) 对所有 mode 都使用单碟 `noise_rms_per_voxel²`，信号也统一用单碟 Gaussian 窗。该表不反馈到 Fisher 求和，不能用表里的干涉“体素方差”解释 σ 的变化。

主单元是 100000 h，最后的辅助对照单元是 10000 h；Copilot 历史显示后者是单独改动的。二者的绝对噪声相差时间因子，不能混读，但这不改变 aux_i/d 的比值。
